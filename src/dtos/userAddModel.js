export default class UserAddModel{
    constructor(firstName, lastName, city,age){
        this.firstName=firstName;
        this.lastName=lastName;
        this.city=city;
        this.age=age;
    }
}